+++
title = "#23197 Add an example demonstrating callbacks with one-shot systems"
date = "2026-03-03T00:00:00"
draft = false
template = "pull_request_page.html"
in_search_index = false

[extra]
current_language = "zh-cn"
available_languages = {"en" = { name = "English", url = "/pull_request/bevy/2026-03/pr-23197-en-20260303" }, "zh-cn" = { name = "中文", url = "/pull_request/bevy/2026-03/pr-23197-zh-cn-20260303" }}
+++

# Title

## 基本信息
- **标题**: Add an example demonstrating callbacks with one-shot systems
- **PR链接**: https://github.com/bevyengine/bevy/pull/23197
- **作者**: alice-i-cecile
- **状态**: 已合并
- **标签**: A-ECS, C-Examples, S-Ready-For-Final-Review, D-Straightforward
- **创建时间**: 2026-03-03T00:08:16Z
- **合并时间**: 2026-03-03T18:08:08Z
- **合并者**: alice-i-cecile

## 描述翻译
### Objective（目标）

这是一个非常强大的模式，但要找到正确的调用方法可能相当具有挑战性！

### Solution（解决方案）

编写一个小型原型，并将其扩展为一个完整的示例！

### Testing（测试）
`cargo run --example callbacks`

## 这个Pull Request的故事

这个PR旨在解决一个在Bevy的ECS（Entity Component System）中使用中遇到的实际问题：如何灵活地存储并与实体关联逻辑，特别是在需要按需执行（one-shot）而非每帧运行的情况下。开发者需要一个清晰、可复用的模式来演示这一技术。

问题的核心在于，Bevy ECS的标准范式是注册在 `App` 上的系统（System）按调度计划（Schedule）运行。然而，在某些场景下，比如过场动画、脚本事件或由UI触发的交互，逻辑需要与特定实体绑定，并且仅在特定条件或事件发生时执行一次。虽然Bevy提供了 `SystemId` 和通过 `Commands` 运行系统的能力，但如何将这些元素组合成一个清晰、实用的模式，对于新手甚至是有经验的开发者来说，可能并不直观。

该PR采用的解决方案直接而有效：创建一个完整的、文档齐全的示例。它没有修改引擎核心代码，而是通过添加示例代码来教育和引导开发者。这种方法风险低，价值高，因为它直接增强了项目的文档和可发现性。

实现分为三个清晰的步骤。首先，在 `Cargo.toml` 中注册新示例，确保其可以被编译和运行，并且被包含在文档生成流程中（`doc-scrape-examples = true`）。其次，更新 `examples/README.md`，为新示例添加索引条目，使其在列表中对使用者可见。最后，也是最重要的部分，创建示例文件 `examples/ecs/callbacks.rs`。

示例代码本身就是一个精心设计的教学工具。它定义了一个 `Callback` 组件，其唯一字段是一个 `SystemId<(), ()>`。这个设计是关键：它将一个可执行系统的引用（ID）存储为实体的数据。在 `setup_callbacks` 函数中，我们看到了三种不同类型的系统如何被注册并存储：
1.  **最简单的闭包系统**：一个没有参数、不查询任何数据的纯函数。
2.  **普通查询系统**：一个可以访问 `Query<&Callback>` 的系统，演示了回调系统如何感知到其他同类组件的存在。
3.  **独占性系统**：一个接收 `&mut World` 参数的系统，展示了最强大的、可以任意修改World的能力。

`run_callbacks` 函数则演示了如何触发这些存储的回调。它遍历所有拥有 `Callback` 组件的实体，并使用 `commands.run_system(callback.system_id)` 来执行对应的系统。代码注释中还提供了一个重要的设计提示：在实际应用中，你可能会使用观察者（Observer）来检测某个实体事件，并以此作为触发回调的时机，这比在 `Update` 中每帧遍历所有回调更高效。

从技术角度看，这个示例揭示了Bevy ECS的灵活性和“系统即数据”的理念。`SystemId` 是一种将系统（逻辑）作为一等公民进行传递和存储的机制。这种模式用一定的运行时开销（需要通过 `Commands` 进行间接调用）换来了极大的架构灵活性。它非常适合那些逻辑执行频率低、但需要与实体紧密绑定、且逻辑本身多样化的场景。

这个PR的影响是直接提升了Bevy项目自身的质量。它为开发者提供了一个官方、标准的模式来解决“按需执行实体关联逻辑”的问题，减少了社区重复发明轮子和摸索正确用法的时间。通过将其作为一个可运行的示例，它确保了代码的正确性和可测试性，同时也成为了项目文档的一部分。

## 视觉表示

```mermaid
graph TD
    subgraph “PR 改动”
        A[“新增示例文件: callbacks.rs”] --> B[“注册到 Cargo.toml”]
        A --> C[“添加到 examples/README.md”]
    end

    subgraph “示例内部结构”
        D[“App (应用程序)”] --> E[“Startup: setup_callbacks”]
        D --> F[“Update: run_callbacks”]
        E --> G[“创建3个携带 Callback 组件的实体”]
        G --> H[“每个 Callback 包含一个 SystemId”]
        F --> I[“遍历并执行 commands.run_system()”]
        I --> H
    end
```

## 关键文件更改

1.  **`examples/ecs/callbacks.rs` (+53/-0)**
    *   **描述与原因**：这是本次PR新增的核心示例文件。它完整演示了如何定义回调组件、注册不同类型的系统到命令中、将系统ID存储在实体上，以及如何遍历并执行这些回调。
    *   **关键代码**：
        ```rust
        // 定义存储系统ID的组件
        #[derive(Component)]
        struct Callback {
            system_id: SystemId<(), ()>,
        }

        // 注册一个可以访问World的独占性系统作为回调
        let exclusive_callback = Callback {
            system_id: commands.register_system(|world: &mut World| {
                let n_entities = world.entities().len();
                println!("This is the exclusive callback system. There are currently {n_entities} entities in the world.");
            }),
        };

        // 执行所有找到的回调
        fn run_callbacks(mut commands: Commands, query: Query<&Callback>) {
            for callback in query.iter() {
                commands.run_system(callback.system_id);
            }
        }
        ```
    *   **与PR目的的关系**：此文件直接实现了PR的目标，即提供一个关于回调与一次性系统的可工作示例。

2.  **`Cargo.toml` (+11/-0)**
    *   **描述与原因**：将新的示例文件 `callbacks.rs` 注册到Bevy的构建系统中，使其可以通过 `cargo run --example callbacks` 命令执行，并配置其元数据（名称、描述、分类），以便在文档和示例列表中被正确展示。
    *   **关键代码**：
        ```toml
        [[example]]
        name = "callbacks"
        path = "examples/ecs/callbacks.rs"
        doc-scrape-examples = true

        [package.metadata.example.callbacks]
        name = "Callbacks"
        description = "Store arbitrary systems in components and run them on demand"
        category = "ECS (Entity Component System)"
        wasm = true
        ```
    *   **与PR目的的关系**：这是使示例可用和可被发现的基础设施配置。

3.  **`examples/README.md` (+1/-0)**
    *   **描述与原因**：在示例目录的索引文件中添加新示例的链接和简短描述，方便开发者浏览和查找可用的ECS示例。
    *   **关键代码**：
        ```markdown
        Example | Description
        --- | ---
        [Callbacks](../examples/ecs/callbacks.rs) | Store arbitrary systems in components and run them on demand
        ```
    *   **与PR目的的关系**：提升示例的文档可发现性，是完善示例贡献的标准步骤。

## 进一步阅读

对于想深入了解此PR所涉及概念的读者，可以参考以下资源：

1.  **Bevy 官方文档 - 系统 (Systems)**: 理解系统是如何定义、注册和运行的基础。
2.  **Bevy 官方文档 - 命令 (Commands)**: 详细了解 `Commands` 系统，特别是 `register_system` 和 `run_system` 方法的工作原理。
3.  **Bevy 官方文档 - 观察者 (Observers)**: 了解如何响应组件变化等事件，这可以与回调模式结合，实现更高效的事件驱动回调触发机制。
4.  **Bevy Cheatbook - Patterns**: 社区维护的秘籍书中可能包含更多高级的ECS模式和最佳实践。
5.  **GitHub PR #23197 的讨论区**: 查看该PR本身的评论和讨论，可能包含作者或审阅者对设计决策的额外见解。

# Full Code Diff
diff --git a/Cargo.toml b/Cargo.toml
index ebced5201b09a..167225b007bb4 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -2280,6 +2280,17 @@ description = "Change detection on components and resources"
 category = "ECS (Entity Component System)"
 wasm = false
 
+[[example]]
+name = "callbacks"
+path = "examples/ecs/callbacks.rs"
+doc-scrape-examples = true
+
+[package.metadata.example.callbacks]
+name = "Callbacks"
+description = "Store arbitrary systems in components and run them on demand"
+category = "ECS (Entity Component System)"
+wasm = true
+
 [[example]]
 name = "component_hooks"
 path = "examples/ecs/component_hooks.rs"
diff --git a/examples/README.md b/examples/README.md
index 56d05ad94bdf9..5a43b3a9d5c95 100644
--- a/examples/README.md
+++ b/examples/README.md
@@ -321,6 +321,7 @@ Example | Description
 
 Example | Description
 --- | ---
+[Callbacks](../examples/ecs/callbacks.rs) | Store arbitrary systems in components and run them on demand
 [Change Detection](../examples/ecs/change_detection.rs) | Change detection on components and resources
 [Component Hooks](../examples/ecs/component_hooks.rs) | Define component hooks to manage component lifecycle events
 [Contiguous Query](../examples/ecs/contiguous_query.rs) | Demonstrates contiguous queries
diff --git a/examples/ecs/callbacks.rs b/examples/ecs/callbacks.rs
new file mode 100644
index 0000000000000..cf1586bf6619e
--- /dev/null
+++ b/examples/ecs/callbacks.rs
@@ -0,0 +1,53 @@
+//! Sometimes, you want an extremely flexible way to store logic associated with an entity.
+//! This example demonstrates how to store arbitrary systems in components and run them on demand.
+//!
+//! This pattern trades some performance for flexibility and works well for things like cutscenes, scripted events,
+//! or one-off UI-driven interactions that don't need to run every frame.
+
+use bevy::{ecs::system::SystemId, prelude::*};
+
+fn main() {
+    let mut app = App::new();
+    app.add_systems(Startup, setup_callbacks);
+    app.add_systems(Update, run_callbacks);
+    app.run();
+}
+
+#[derive(Component)]
+struct Callback {
+    system_id: SystemId<(), ()>,
+}
+
+fn setup_callbacks(mut commands: Commands) {
+    let trivial_callback = Callback {
+        system_id: commands.register_system(|| {
+            println!("This is the trivial callback system");
+        }),
+    };
+
+    let ordinary_system_callback = Callback {
+        system_id: commands.register_system(|query: Query<&Callback>| {
+            let n_callbacks = query.iter().len();
+            println!("This is the ordinary callback system. There are currently {n_callbacks} callbacks in the world.");
+        }),
+    };
+
+    let exclusive_callback = Callback {
+        system_id: commands.register_system(|world: &mut World| {
+            let n_entities = world.entities().len();
+            println!("This is the exclusive callback system. There are currently {n_entities} entities in the world.");
+        }),
+    };
+
+    commands.spawn(trivial_callback);
+    commands.spawn(ordinary_system_callback);
+    commands.spawn(exclusive_callback);
+}
+
+// In many cases, you might want to use an observer to detect when a callback should run,
+// triggering the callback in response to some entity-event!
+fn run_callbacks(mut commands: Commands, query: Query<&Callback>) {
+    for callback in query.iter() {
+        commands.run_system(callback.system_id);
+    }
+}