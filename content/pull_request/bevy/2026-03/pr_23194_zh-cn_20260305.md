+++
title = "#23194 Fix instability in `sample_visible_ggx` for normals near -Z"
date = "2026-03-05T00:00:00"
draft = false
template = "pull_request_page.html"
in_search_index = false

[extra]
current_language = "zh-cn"
available_languages = {"en" = { name = "English", url = "/pull_request/bevy/2026-03/pr-23194-en-20260305" }, "zh-cn" = { name = "中文", url = "/pull_request/bevy/2026-03/pr-23194-zh-cn-20260305" }}
+++

# 分析报告：修复法线接近-Z时`sample_visible_ggx`的不稳定性问题

## 基本信息
- **标题**: Fix instability in `sample_visible_ggx` for normals near -Z
- **PR链接**: https://github.com/bevyengine/bevy/pull/23194
- **作者**: dylansechet
- **状态**: 已合并
- **标签**: A-Rendering, S-Ready-For-Final-Review, M-Deliberate-Rendering-Change, C-Refinement
- **创建时间**: 2026-03-02T20:10:02Z
- **合并时间**: 2026-03-04T23:07:31Z
- **合并者**: alice-i-cecile

## 描述翻译

### 目标

`sample_visible_ggx`函数中执行了`normal.z + 1`的除法操作，当输入法线接近(0, 0, -1)时会产生无效值。这导致`GeneratedEnvironmentMapLight`中出现伪影，在白炉测试中可见。

### 解决方案

采用https://bruop.github.io/ibl中描述的方法，当法线接近±Z时使用带有特殊情况的TBN矩阵，而不是当前实现的反射方法。

### 测试

- 在main分支和我的分支上运行`cargo run --example reflection_probes`，确保没有明显差异
- 运行`cargo run --example testbed_white_furnace`，检查伪影是否消失

---

### 展示

白炉测试（同时应用了#23189）：

**修复前：**
<img width="1280" height="720" alt="before" src="https://github.com/user-attachments/assets/8fa830a7-7c4e-46a8-a2de-f598af04e9a1" />

**修复后：**
<img width="1280" height="720" alt="after" src="https://github.com/user-attachments/assets/0144f3fd-b4d8-43e3-a4da-6882b79e90f7" />

---

## 本次PR的技术分析

### 问题背景与根本原因

在物理渲染中，`sample_visible_ggx`函数用于生成环境光照采样，是图像光照(Image-based Lighting, IBL)系统的关键组件。原实现存在一个数值稳定性问题：当表面法线接近负Z轴方向(0, 0, -1)时，代码中的除法操作`wr.z`可能产生极大的值或NaN。

问题出现在以下代码段：
```wgsl
let wr = n + up;  // up = vec3f(0.0, 0.0, 1.0)
let c = dot(wr, c_std) * wr / wr.z - c_std;
```

当`n`接近(0, 0, -1)时，`wr.z`趋近于0，导致除法结果趋于无穷大，进而产生渲染伪影。这种边缘情况在球形表面渲染时尤其明显，特别是环境贴图采样中。

### 技术解决方案

作者采用了来自[bruop.github.io/ibl](https://bruop.github.io/ibl)的技术方案，将原来的反射方法替换为基于TBN矩阵的正交化方法。

原方法试图通过反射操作将采样向量对齐到+Z轴，但存在数值不稳定问题。新方法使用`orthonormalize`函数构建一个切线-副切线-法线(TBN)矩阵，然后通过矩阵乘法旋转采样向量。

### 实现细节与代码分析

核心改动在`crates/bevy_pbr/src/render/pbr_lighting.wgsl`文件中：

1. **导入语句更新**：新增对`orthonormalize`函数的导入
```wgsl
// 之前：
#import bevy_render::maths::PI

// 之后：
#import bevy_render::maths::{PI, orthonormalize}
```

2. **采样算法重构**：完全替换了向量对齐逻辑
```wgsl
// 之前的不稳定实现：
// Reflect the sample so that the normal aligns with +Z
let up = vec3f(0. 0, 0.0, 1.0);
let wr = n + up;
let c = dot(wr, c_std) * wr / wr.z - c_std;

// 之后的稳定实现：
// Rotate the sample so that the normal aligns with +Z
let TBN = orthonormalize(n);
let c = TBN * c_std;
```

新实现的关键优势：
- **数值稳定性**：避免除以接近零的值
- **数学正确性**：使用正交化矩阵确保旋转操作保持向量长度不变
- **代码简洁性**：逻辑更清晰，减少了计算步骤

### 正交化方法的技术说明

`orthonormalize(n)`函数构造一个3×3正交矩阵，其中第三列（在WGSL中可能是第三行，取决于存储布局）是归一化的输入法线`n`。这个矩阵将一个以法线为Z轴的正交坐标系映射到标准坐标系(0,0,1)。

这种方法的优势在于：
1. 避免了原反射方法中分母接近零的问题
2. 提供了一种更通用的坐标变换框架
3. 在数学上更稳定，特别是在法线接近极点的边缘情况下

### 测试验证与质量保证

作者执行了系统性测试来验证修复效果：
1. **功能回归测试**：通过`reflection_probes`示例确保核心功能不受影响
2. **问题专项测试**：使用`testbed_white_furnace`示例专门验证伪影是否消除

白炉测试对比图清楚展示了修复效果：修复前的图像在法线接近-Z区域出现明显的亮斑和噪声，而修复后的图像表现出均匀的辐射分布，符合物理正确性。

### 工程意义与最佳实践

这个修复展示了几个重要的工程实践：

1. **边缘情况处理**：渲染算法必须考虑所有可能的输入范围，特别是数值不稳定的边界条件
2. **数值稳定性优先**：在计算机图形学中，数值稳定性通常比理论简洁性更重要
3. **引用外部权威资源**：作者引用了行业认可的IBL实现方案，增加了解决方案的可信度
4. **最小化修改**：修复仅涉及必要的代码变更，保持了系统的简洁性

### 潜在影响与后续考虑

虽然这个修复解决了当前问题，但在更广泛的上下文中需要考虑：

1. **性能影响**：`orthonormalize`函数可能比原来的反射操作更复杂，需要评估性能变化
2. **GPU兼容性**：正交化算法在不同GPU架构上的表现一致性
3. **扩展性**：新的TBN方法为后续的渲染优化提供了更灵活的框架

## 可视化组件关系

```mermaid
graph TD
    A[环境光照采样<br/>sample_visible_ggx] --> B{法线方向判断}
    B -->|接近±Z轴| C[TBN矩阵正交化<br/>orthonormalize(n)]
    B -->|其他方向| D[原有采样逻辑]
    C --> E[矩阵旋转<br/>TBN * c_std]
    E --> F[半向量计算<br/>wm_std]
    D --> F
    F --> G[GGX重要性采样]
    G --> H[环境贴图生成<br/>GeneratedEnvironmentMapLight]
```

## 关键文件变更

### `crates/bevy_pbr/src/render/pbr_lighting.wgsl` (+4/-5)

**变更描述**：
修复了`sample_visible_ggx`函数在处理接近负Z轴法线时的数值不稳定问题，将原来的反射对齐方法替换为基于TBN矩阵的正交化方法。

**核心代码变更**：
```wgsl
// 变更前：
#import bevy_render::maths::PI

// 在sample_visible_ggx函数中：
// Reflect the sample so that the normal aligns with +Z
let up = vec3f(0.0, 0.0, 1.0);
let wr = n + up;
let c = dot(wr, c_std) * wr / wr.z - c_std;

// 变更后：
#import bevy_render::maths::{PI, orthonormalize}

// 在sample_visible_ggx函数中：
// Rotate the sample so that the normal aligns with +Z
let TBN = orthonormalize(n);
let c = TBN * c_std;
```

**与PR目的的关系**：
这个变更是本次PR的核心修复，直接解决了法线接近-Z时除法溢出的问题，消除了白炉测试中的渲染伪影。

## 进一步阅读

### 相关技术资源：
1. **基于图像的照明(IBL)**：
   - [Real Shading in Unreal Engine 4](https://cdn2.unrealengine.com/Resources/files/2013SiggraphPresentationsNotes-26915738.pdf)
   - [Physically Based Shading at Disney](https://blog.selfshadow.com/publications/s2012-shading-course/burley/s2012_pbs_disney_brdf_notes_v3.pdf)

2. **数值稳定性与计算机图形学**：
   - [Numerical Recipes: The Art of Scientific Computing](https://numerical.recipes/)
   - [Graphics Gems系列书籍](https://www.realtimerendering.com/resources/GraphicsGems/)

3. **WGSL着色器编程**：
   - [WebGPU Shading Language规范](https://www.w3.org/TR/WGSL/)
   - [Bevy引擎的着色器开发指南](https://bevyengine.org/learn/quick-start/getting-started/systems/)

4. **物理渲染理论与实践**：
   - [PBRT: Physically Based Rendering](https://www.pbr-book.org/)
   - [Real-Time Rendering](https://www.realtimerendering.com/)