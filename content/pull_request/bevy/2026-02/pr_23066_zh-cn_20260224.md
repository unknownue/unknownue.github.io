+++
title = "#23066 Makes Free Camera speed change exponential wrt scroll"
date = "2026-02-24T00:00:00"
draft = false
template = "pull_request_page.html"
in_search_index = false

[extra]
current_language = "zh-cn"
available_languages = {"en" = { name = "English", url = "/pull_request/bevy/2026-02/pr-23066-en-20260224" }, "zh-cn" = { name = "中文", url = "/pull_request/bevy/2026-02/pr-23066-zh-cn-20260224" }}
+++

# 标题

## 基本信息
- **标题**: Makes Free Camera speed change exponential wrt scroll
- **PR链接**: https://github.com/bevyengine/bevy/pull/23066
- **作者**: kfc35
- **状态**: 已合并
- **标签**: C-Usability, S-Ready-For-Final-Review, A-Math, D-Straightforward, A-Camera
- **创建时间**: 2026-02-20T06:12:13Z
- **合并时间**: 2026-02-24T01:50:21Z
- **合并人**: alice-i-cecile

## 描述翻译
**目标 (Objective)**
- 采纳并关闭 #21453，选择使用 `exp` 的方案
- 取代并关闭 #21483
- 使 free_camera 在极大或极小的尺度下更易用

**解决方案 (Solution)**
- 基本上更新了 #21453，使用 `exp` 替代 `powf` 和百分比变化。这意味着 `scroll_factor` 不再表示"速度增加/减少的额外百分比"。
- 我确保在 `scroll_factor` 中添加了一些有用的注释，用于确定适当的 `scroll_factor` 值以及合理的默认值。希望这些注释不会显得过于冗长。

**测试 (Testing)**
我使用 `cargo run --example free_camera_controller --features=“free_camera”` 和 `cargo run --example 3d_gizmos --features=“free_camera”` 进行了测试，确保控制感良好。与 `main` 分支相比，达到高速所需时间不会过长，并且降低到低于初始速度的过程更加平缓。

## 这个 Pull Request 的故事

这个 PR 解决了 Bevy 引擎中自由相机控制器的一个实际问题：鼠标滚轮对相机速度的控制在极端尺度下不够直观和实用。

问题的根源在于现有的实现使用了线性缩放。当用户通过滚轮调整相机速度时，系统采用加法模式：每次滚轮事件都会给 `speed_multiplier` 加上 `scroll * scroll_factor`。这种方法存在两个主要缺陷。首先，在处理不同硬件时，滚轮事件可能以不同粒度报告（如单次大滚动或多次小滚动），导致速度变化不一致。其次，在线性模型下，要跨越多个数量级的尺度变化需要大量滚动操作，这在处理大型场景时效率很低。

开发者面临的核心技术挑战是为速度控制找到一种既符合用户直觉又具备数学一致性的模型。之前有两个相关 PR 提出了不同方案：#21453 使用 `powf` 实现百分比变化，而 #21483 尝试了其他方法。这个 PR 选择采用指数函数方案，因为它能优雅地解决一致性问题。

解决方案的核心是将速度乘数的更新从加法改为乘法。具体来说，对于每个单位的滚动，速度乘数乘以 `e^(scroll_factor * scroll)`。这种指数变化确保了无论滚轮事件如何分块，相同的总滚动量都会产生相同的速度变化比例。这在数学上是可靠的，因为连续的滚动事件会连续地应用指数函数，这与单次大滚动的效果在数学上是一致的。

实现的关键部分在 `run_freecamera_controller` 函数中。之前的代码使用累加的方式：
```rust
state.speed_multiplier += scroll * config.scroll_factor;
state.speed_multiplier = state.speed_multiplier.clamp(0.0, f32::MAX);
```

新的实现改为：
```rust
state.speed_multiplier *= exp(config.scroll_factor * scroll);
state.speed_multiplier = state.speed_multiplier.clamp(f32::EPSILON, f32::MAX);
```

这里有几个重要的技术细节。首先，`exp` 函数（自然指数函数）的使用确保了乘法的一致性。其次，将下限从 `0.0` 改为 `f32::EPSILON` 是必要的，因为指数函数永远不会产生零值，而零乘数会使相机完全无法移动。这个微小的调整保持了数学正确性。

`scroll_factor` 的默认值也从 `0.5` 改为约 `0.04879016`，这是 `ln(1.05)` 的近似值。这意味着默认情况下，每次正向滚动单位将速度增加约 5%。这个值的选择经过考虑，提供了平滑的速度调整体验。注释中详细解释了如何选择 `scroll_factor` 值，建议范围在 `ln(1.05)` 到 `ln(1.1)` 之间，对应每次滚动 5% 到 10% 的速度变化。

从工程角度来看，这个改变是向后兼容的，因为它没有改变 API 结构，只是改变了 `scroll_factor` 的语义和默认值。开发者可以通过调整 `scroll_factor` 来获得期望的滚动灵敏度。指数模型的一个显著优势是它自然地处理了尺度跨越问题：要增加 10 倍速度，无论当前速度是多少，都需要大约 `ln(10)/scroll_factor` 次的滚动单位，这在大型场景导航时特别有用。

测试验证了改进的实际效果。与之前版本相比，新实现在达到高速时不需要过多的滚动操作，而在降低速度时也提供了更渐进的反馈。这使得自由相机控制器在从微尺度到宏观尺度的各种场景中都更加实用。

这个 PR 展示了如何通过数学模型的选择来解决用户界面的一致性问题。指数函数的应用不仅解决了硬件事件分块的问题，还为跨越多个数量级的参数调整提供了自然的控制方式。对于游戏引擎和交互式 3D 应用来说，这种改进虽然看似微小，但对用户体验有显著提升。

## 可视化表示

```mermaid
graph TD
    A[鼠标滚轮输入] --> B{滚动事件处理}
    B --> C[计算滚动量 scroll]
    C --> D[计算指数因子 exp(scroll_factor * scroll)]
    D --> E[更新速度乘数 speed_multiplier *= 指数因子]
    E --> F[钳制到有效范围 EPSILON, MAX]
    F --> G[应用于相机移动速度]
```

## 关键文件更改

- `crates/bevy_camera_controller/src/free_camera.rs` (+27/-10)

这个文件包含了自由相机控制器的核心实现。主要更改集中在 `scroll_factor` 的定义和鼠标滚轮处理逻辑上。

**变更详情:**

1. **导入变更**: 添加了 `bevy_math::ops::exp` 的导入以使用指数函数。

2. **scroll_factor 字段注释增强**: 添加了详细的文档注释，解释了指数的行为和合理的默认值范围。

```rust
// 之前:
/// Multiplier for how the mouse scroll wheel modifies [`walk_speed`](FreeCamera::walk_speed)
/// and [`run_speed`](FreeCamera::run_speed).
pub scroll_factor: f32,

// 之后:
/// Multiplier for how much the mouse scroll wheel affects [`walk_speed`](FreeCamera::walk_speed)
/// and [`run_speed`](FreeCamera::run_speed).
///
/// Mouse scroll affects speed exponentially. This is to ensure that scrolling the same
/// amount always has the same effect on speed, regardless of how the scroll amount
/// is reported by the hardware (i.e. as one big event vs many smaller events). This
/// also allows the free camera to navigate very large scenes easier.
///
/// For every unit of scroll, the speed of the camera is multiplied by a factor of
/// `e^(scroll_factor)`.
///
/// A reasonable value to start with is a `scroll_factor` between 0.04879016 (~ln(1.05))
/// and 0.0953102 (~ln(1.1)). They represent an increase by a factor between 1.05 and 1.1 per
/// positive unit scroll and a reduction between ~0.952 (~e^-0.04879016) and ~0.909
/// (~e^-0.0953102) times its value per negative unit scroll
///
/// A `scroll_factor` closer to 0.0 means that speed will be less sensitive to scroll.
/// A `scroll_factor` equal to 0.0 means that speed is unaffected by scroll
/// (it will be multiplied by a factor of 1.0 per positive and negative unit scroll).
pub scroll_factor: f32,
```

3. **默认值变更**: `scroll_factor` 的默认值从 `0.5` 改为 `0.04879016`。

```rust
// 之前:
scroll_factor: 0.5,

// 之后:
// Approximation of ln(1.05)
scroll_factor: 0.04879016,
```

4. **滚轮处理逻辑变更**: 从加法模型改为乘法指数模型。

```rust
// 之前:
let mut scroll = 0.0;

let amount = match accumulated_mouse_scroll.unit {
    MouseScrollUnit::Line => accumulated_mouse_scroll.delta.y,
    MouseScrollUnit::Pixel => {
        accumulated_mouse_scroll.delta.y / MouseScrollUnit::SCROLL_UNIT_CONVERSION_FACTOR
    }
};
scroll += amount;
state.speed_multiplier += scroll * config.scroll_factor;
// Clamp the speed multiplier for safety
state.speed_multiplier = state.speed_multiplier.clamp(0.0, f32::MAX);

// 之后:
let scroll = match accumulated_mouse_scroll.unit {
    MouseScrollUnit::Line => accumulated_mouse_scroll.delta.y,
    MouseScrollUnit::Pixel => {
        accumulated_mouse_scroll.delta.y / MouseScrollUnit::SCROLL_UNIT_CONVERSION_FACTOR
    }
};
// By using exponentiation we ensure that this scales up and down smoothly
// regardless of the amount of scrolling processed per frame
state.speed_multiplier *= exp(config.scroll_factor * scroll);
// Clamp the speed multiplier for safety.
state.speed_multiplier = state.speed_multiplier.clamp(f32::EPSILON, f32::MAX);
```

这些变更共同实现了更一致和可扩展的相机速度控制，特别优化了大型场景的导航体验。

## 延伸阅读

对于想要了解更多相关概念的读者，建议参考以下资源：

1. **指数函数和对数函数**: 理解 `exp` 和 `ln` 函数的数学性质，特别是它们如何描述比例变化。
2. **人机交互中的控制模型**: 研究不同的输入映射方法，特别是对于连续参数调整的最佳实践。
3. **Bevy 输入系统**: 了解 Bevy 如何处理鼠标、键盘和其他输入事件。
4. **3D 相机控制系统**: 学习游戏和图形应用中相机控制的各种技术和模式。
5. **浮点数精度**: 理解 `f32::EPSILON` 的意义以及在数值计算中防止下溢/上溢的技术。

# 完整代码差异
```
diff --git a/crates/bevy_camera_controller/src/free_camera.rs b/crates/bevy_camera_controller/src/free_camera.rs
index 89b655bd02d7f..939c4ea1c5f55 100644
--- a/crates/bevy_camera_controller/src/free_camera.rs
+++ b/crates/bevy_camera_controller/src/free_camera.rs
@@ -23,7 +23,7 @@ use bevy_input::mouse::{
 };
 use bevy_input::ButtonInput;
 use bevy_log::info;
-use bevy_math::{EulerRot, Quat, StableInterpolate, Vec2, Vec3};
+use bevy_math::{ops::exp, EulerRot, Quat, StableInterpolate, Vec2, Vec3};
 use bevy_time::{Real, Time};
 use bevy_transform::prelude::Transform;
 use bevy_window::{CursorGrabMode, CursorOptions, Window};
@@ -94,8 +94,25 @@ pub struct FreeCamera {
     pub walk_speed: f32,
     /// Base multiplier for running translation speed.
     pub run_speed: f32,
-    /// Multiplier for how the mouse scroll wheel modifies [`walk_speed`](FreeCamera::walk_speed)
+    /// Multiplier for how much the mouse scroll wheel affects [`walk_speed`](FreeCamera::walk_speed)
     /// and [`run_speed`](FreeCamera::run_speed).
+    ///
+    /// Mouse scroll affects speed exponentially. This is to ensure that scrolling the same
+    /// amount always has the same effect on speed, regardless of how the scroll amount
+    /// is reported by the hardware (i.e. as one big event vs many smaller events). This
+    /// also allows the free camera to navigate very large scenes easier.
+    ///
+    /// For every unit of scroll, the speed of the camera is multiplied by a factor of
+    /// `e^(scroll_factor)`.
+    ///
+    /// A reasonable value to start with is a `scroll_factor` between 0.04879016 (~ln(1.05))
+    /// and 0.0953102 (~ln(1.1)). They represent an increase by a factor between 1.05 and 1.1 per
+    /// positive unit scroll and a reduction between ~0.952 (~e^-0.04879016) and ~0.909
+    /// (~e^-0.0953102) times its value per negative unit scroll
+    ///
+    /// A `scroll_factor` closer to 0.0 means that speed will be less sensitive to scroll.
+    /// A `scroll_factor` equal to 0.0 means that speed is unaffected by scroll
+    /// (it will be multiplied by a factor of 1.0 per positive and negative unit scroll).
     pub scroll_factor: f32,
     /// Friction factor used to exponentially decay [`velocity`](FreeCameraState::velocity) over time.
     pub friction: f32,
@@ -116,7 +133,8 @@ impl Default for FreeCamera {
             keyboard_key_toggle_cursor_grab: KeyCode::KeyM,
             walk_speed: 5.0,
             run_speed: 15.0,
-            scroll_factor: 0.5,
+            // Approximation of ln(1.05)
+            scroll_factor: 0.04879016,
             friction: 40.0,
         }
     }
@@ -230,18 +248,17 @@ pub fn run_freecamera_controller(
         return;
     }
 
-    let mut scroll = 0.0;
-
-    let amount = match accumulated_mouse_scroll.unit {
+    let scroll = match accumulated_mouse_scroll.unit {
         MouseScrollUnit::Line => accumulated_mouse_scroll.delta.y,
         MouseScrollUnit::Pixel => {
             accumulated_mouse_scroll.delta.y / MouseScrollUnit::SCROLL_UNIT_CONVERSION_FACTOR
         }
     };
-    scroll += amount;
-    state.speed_multiplier += scroll * config.scroll_factor;
-    // Clamp the speed multiplier for safety
-    state.speed_multiplier = state.speed_multiplier.clamp(0.0, f32::MAX);
+    // By using exponentiation we ensure that this scales up and down smoothly
+    // regardless of the amount of scrolling processed per frame
+    state.speed_multiplier *= exp(config.scroll_factor * scroll);
+    // Clamp the speed multiplier for safety.
+    state.speed_multiplier = state.speed_multiplier.clamp(f32::EPSILON, f32::MAX);
 
     // Handle key input
     let mut axis_input = Vec3::ZERO;
```