%YAML 1.2
---
# Mermaid syntax definition (minimal, just to prevent highlighting)
name: mermaid
file_extensions: [mermaid]
scope: source.mermaid

contexts:
  main:
    - match: ''
      push: content

  content:
    - match: .+
      scope: text.mermaid 